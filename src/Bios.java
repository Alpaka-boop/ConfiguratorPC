public class Bios {
    private BiosType type;
    private BiosVersion version;

    public Bios(BiosType type, BiosVersion version) {
        this.type = type;
        this.version = version;
    }
}
